<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="10dp"
    app:cardElevation="2dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Kiri: Mood Icon -->
        <ImageView
            android:id="@+id/iv_mood_icon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_mood_happy_extreme"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:contentDescription="Mood" />

        <!-- Kiri Tengah: Judul Mood -->
        <TextView
            android:id="@+id/tv_mood_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@android:color/black"
            android:layout_marginStart="16dp"
            app:layout_constraintTop_toTopOf="@id/iv_mood_icon"
            app:layout_constraintStart_toEndOf="@id/iv_mood_icon"
            tools:text="Happy" />

        <!-- Kanan Atas: Tanggal -->
        <TextView
            android:id="@+id/tv_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            app:layout_constraintTop_toTopOf="@id/tv_mood_name"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="14 October 2025" />

        <!-- Tengah: Ringkasan Jurnal -->
        <TextView
            android:id="@+id/tv_content_summary"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:maxLines="2"
            android:ellipsize="end"
            app:layout_constraintTop_toBottomOf="@id/tv_mood_name"
            app:layout_constraintStart_toStartOf="@id/tv_mood_name"
            app:layout_constraintEnd_toStartOf="@id/iv_journal_image_preview"
            tools:text="Today I feel more relieved after meditating and I found a cute cat on the street." />

        <!-- Kanan Bawah: Preview Gambar (Jika Ada) -->
        <ImageView
            android:id="@+id/iv_journal_image_preview"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:scaleType="centerCrop"
            android:layout_marginStart="16dp"
            android:visibility="gone"
            tools:visibility="visible"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/tv_date"
            android:contentDescription="Gambar Jurnal" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</com.google.android.material.card.MaterialCardView>