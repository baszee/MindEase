<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar"> <!-- Perubahan tema di sini -->

        <!-- 1. DEKLARASI ACTIVITY UTAMA (LAUNCHER) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. DEKLARASI ACTIVITY BARU SESUAI LANGKAH 18.4b -->
        <!-- Pastikan Anda menambahkan tag <activity> baru di sini. -->
        <activity
            android:name=".NamaActivityBaru"
            android:label="Judul Layar Baru"
            android:exported="false"
            android:parentActivityName=".MainActivity">
            <!--
            android:exported="false" disarankan untuk activity non-launcher.
            android:parentActivityName=".MainActivity" akan menambahkan tombol 'Back' ke MainActivity.
            -->
        </activity>

    </application>
</manifest>